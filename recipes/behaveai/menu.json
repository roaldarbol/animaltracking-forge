{
  "$schema": "https://schemas.conda.org/menuinst/menuinst-1-1-0.schema.json",
  "menu_name": "BehaveAI",
  "menu_items": [
    {
      "name": "BehaveAI",
      "description": "Animal tracking and behaviour classification using YOLO and motion analysis",
      "command": ["{{ PREFIX }}/bin/behaveai"],
      "platforms": {
        "linux": {
          "command": ["{{ PREFIX }}/bin/behaveai"],
          "Categories": ["Science"]
        },
        "osx": {
          "command": ["{{ PREFIX }}/bin/behaveai"]
        },
        "win": {
          "command": ["{{ PREFIX }}/Scripts/behaveai.exe"]
        }
      }
    }
  ]
}